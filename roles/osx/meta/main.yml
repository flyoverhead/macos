---

galaxy_info:
  role_name: osx
  namespace: flyoverhead
  author: flyoverhead
  description: macOS system preferences via the defaults database
  license: GPL-3.0-only
  min_ansible_version: '2.16'
  platforms:
    - name: MacOSX
      versions:
        - all
  galaxy_tags:
    - defaults
    - macos
    - preferences

dependencies: []
